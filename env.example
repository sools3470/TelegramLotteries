# Environment Variables for cPanel Deployment

# Database Connection (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database_name

# Session Secret (Generate a strong random string)
SESSION_SECRET=your-very-secure-random-string-minimum-32-characters

# Application Environment
NODE_ENV=production

# Application Port (Usually set by hosting provider)
PORT=3000

# Example PostgreSQL connection strings:
# Local: postgresql://user:password@localhost:5432/raffle_db
# Cloud: postgresql://user:password@hostname.com:5432/database_name